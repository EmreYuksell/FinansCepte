server:
  port: 8081

spring:
  application:
    name: service-user
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/finanscepte_user}
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:mongo}
